#!groovy

@Library("Infrastructure")
def type = "java"          // supports "java", "nodejs" and "angular"

def product = "darts"

def component = "darts-backend" // must match infrastructure module name

withPipeline(type, product, component) {
  enableSlackNotifications('#darts-builds')
}