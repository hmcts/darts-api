#!groovy

@Library("Infrastructure")

def type = "java"
def product = "darts"
def component = "api"

def branchesToSync = ['demo','sbox']

withPipeline(type, product, component) {
      enableSlackNotifications('#darts-builds')
      syncBranchesWithMaster(branchesToSync)
}
